# Iris 3.1 需求清单

> 基于 Iris v2.7.1，按三步骤分步实施
> **状态：步骤 1 ✅ / 步骤 2 ✅ / 步骤 3 ⏳ 待开始**
> 仓库名称：**iris3**
> 路径约定：
>   - 数据源：`${IRIS_WORK_DOCS_DIR}` → `.../WORKSPACE/WIKI-ROOT/SOURCE/`
>   - Wiki 输出：`${IRIS_WIKI_ROOT}` → `.../WORKSPACE/WIKI-ROOT/LLM-WIKI/`

---

## 配置安全方案（全局）

复用 v2.7.1 四层配置体系，适合开源：

```
优先级：OS 环境变量 > .env 文件 > macOS Keychain > 保留占位符
```

| 层 | 文件 | 版本控制 | 说明 |
|----|------|---------|------|
| ① | `.env` | ❌ gitignored | API Key、路径变量 |
| ② | `config/*.json` | ❌ gitignored | 用户配置（从 .example 复制） |
| ③ | `config/*.json.example` | ✅ 版本控制 | 配置模板（占位值） |
| ④ | `data/` | ❌ 全 gitignore | 运行时数据 |

---

## 步骤 1：最小化迁移 — 非知识库能力迁移 ✅ 已完成

**目标**：从 v2.7.1 迁移完全不依赖知识库（Wiki）的能力，在新项目中先搭起可运行的骨架。
**技术栈**：Python 3.9+
**飞书接口**：lark-cli

### 1.1 核心基础设施

| 需求 | 说明 | 来源 |
|------|------|------|
| 项目骨架 | `pyproject.toml`、目录结构、入口点 | 新建 |
| 配置加载 | `.env` 加载、`${VAR}` 变量解析、`config/*.json` + `.example` 回退 | `src/iris/config/loader.py` |
| 密钥链集成 | macOS Keychain 存取 API Key | `src/iris/config/secrets.py` |
| LLM Provider + 路由 | 多 Provider 注册、降级链、路由规则引擎 | `src/iris/llm/` |
| 日志系统 | 结构化 JSON 日志 | `src/iris/utils/logging.py` |
| 核心抽象层 | Protocol、文件锁、写入守卫、存储层 | `src/iris/core/` |

### 1.2 命令行基础设施

| 命令 | 说明 | 处置 |
|------|------|------|
| `check-config` | 检查配置完整性（去掉 Wiki 相关检查项） | ✅ 迁移(裁剪) |
| `route-model` | 模型路由查看/调试 | ✅ 迁移 |
| `status` | 系统状态（去掉 Wiki 统计） | ✅ 迁移(裁剪) |
| `diagnose` | 系统诊断（去掉 Wiki 诊断项） | ✅ 迁移(裁剪) |
| `agent-spec` | Agent 能力规范输出 | ✅ 迁移 |
| `secrets-set/list/delete` | 密钥链管理 | ✅ 迁移 |

### 1.3 记忆系统

| 命令 | 说明 | 处置 |
|------|------|------|
| `memory-status/list/delete` | 记忆状态、列表、删除 | ✅ 迁移 |
| `memory-maintenance` | 记忆自治维护（老化、冲突检测、碎片摘要） | ✅ 迁移 |
| `memory-export/import` | 记忆导入导出 | ✅ 迁移 |
| `sync-memory` | 系统记忆同步到 Iris | ✅ 迁移 |
| `working-set/show/clear` | 工作上下文管理 | ✅ 迁移 |

### 1.4 独立工具

| 命令 | 说明 | 处置 |
|------|------|------|
| `process` | 图文混合双阶段处理（adv_model → base_model） | ✅ 迁移 |
| `chat`（可选） | 简单对话模式入口 | 可扩展 |

### 1.5 不依赖知识库的第三方集成

| 命令 | 说明 | 处置 |
|------|------|------|
| `trello` | 看板 CRUD、标签管理、LLM 汇总 | ✅ 迁移 |
| `extract-weekly-reports` | 飞书邮箱周报提取，输出到 `SOURCE/成员周报/`（步骤 2 接入 SOURCE 扫描） | ✅ 迁移 |
| `extract-didi-travel` | 滴滴行程提取（保留） | ✅ 迁移 |

### 1.6 步骤 1 不迁移 — 等待步骤 2/3

| 能力 | 原因 |
|------|------|
| `scan-source` / `build-chunks` / `build-vector-index` | 文档处理管道——检索基础，将在步骤 2 适配新 KB 结构 |
| `search` / `ask` | 通过 `EnhancedRetriever` 依赖 `WikiSearcher` |
| `build-report` / `build-mindmap` | 通过 `EnhancedRetriever` 依赖 KB |
| `build-biweekly-report` | `WikiSearcher` 查询概念边界进行素材过滤 |
| `daily-start` | 包含 Wiki 自动发现步骤 |
| `transcribe-meeting` / `batch-transcribe` | Wiki 用于 ASR 校正（加载领域词汇） |
| `build-asr-prompt` | 从 Wiki 提取领域词汇 |
| Wiki 全套命令 | `discover-wiki` / `build-wiki` / `build-wiki-nav` / `wiki-pipeline` |
| 飞书团队知识库操作 | 全新能力，步骤 3 |

---

## 步骤 2：本地知识库重构 ⭐⭐ ✅ 已完成

> **实际完成：** 30 页 Wiki（领域6/概念7/项目8/人物9），594 源文档，3,402 chunks，5,810 向量索引，59 单元测试，6/6 质量绿标

**目标**：重建数据源结构、Wiki 体系（参考 Karpathy LLM Wiki 模式），迁移 v2.7.1 的 Wiki 数据，重构所有依赖知识库的能力。
**ASR 校正**：词汇来源定位为本地 Wiki。

### 2.1 知识库路径设计

```
Obsidian 仓库：.../WORKSPACE/
                ├── WIKI-ROOT/
                │   ├── SOURCE/          ← 个人知识库原始文档存储地
                │   │                     （人写入 + Iris 自动归档）
                │   └── LLM-WIKI/        ← Wiki 输出：结构化知识页面（LLM 写入）
                ├── @转转/               ← 其他工作文档（保留不动）
                └── xiaolongxia/         ← v2.7.1 旧工作区（保留不动）
```

### 2.2 SOURCE 目录设计（原始文档存储地）

**定位**：个人知识库的原始文档存储地。人日常写入新文档，Iris 也能自动归档文档到合适的文件夹。
**设计原则**：按内容类型分类，文件名自带日期，扁平易维护。

```
SOURCE/
├── _INDEX.md                        # 目录说明（可选）
│
├── 我的周报/                        # 自己写的周报（手动放入）
│   └── 20260626-第26周-工作周报.md
│
├── 成员周报/                        # 团队成员的周报（extract-weekly-reports 自动提取）
│   └── 2026-w26/
│       ├── 张三-2026w26.md
│       └── 李四-2026w26.md
│
├── 会议纪要/                        # 各类会议记录
│   ├── 20260625-技术评审-搜索推荐架构.md
│   └── 20260624-周例会-技术研发部.md
│
├── 讨论思考/                        # 进行中的思考、分析、头脑风暴
│   └── 20260625-某检测项目下半年规划-draft.md
│
├── 方案报告/                        # 正式方案、汇报材料、蓝图、分析报告
│   └── 质检自动化蓝图-v1.md
│
├── 项目/                            # 项目级文档（按项目名子目录分组）
│   ├── xray-detection/
│   │   └── 技术方案-v2.md
│   └── 视频审核/
│       └── 深圳仓试点方案.md
│
├── 参考资料/                        # 行业分享、外部文章、论文笔记
│   └── 多任务学习论文笔记.md
│
├── 关键信息/                        # 重要事实、数据、决策的快照摘录
│   └── AI评估故障-GUCCI误判.md
│
└── 草稿/                            # 临时笔记、未分类内容（可随时清理）
    └── first-test.md
```

**自动归档需求**：未来 Iris 应能自动将文档归入 SOURCE 的合适文件夹（如通过 CLI 命令 `archive-to-source`），分析文档内容后自动判断类别并放入对应目录。

### 2.3 数据源层

| 需求 | 说明 |
|------|------|
| 数据源配置 | `config/data_source.json`，指向 `SOURCE/` 目录，复用 v2.7.1 的扫描/切块逻辑但适配新路径 |
| `scan-source` | 扫描 `SOURCE/` 下的原始 Markdown 文档（增量扫描） |
| `build-chunks` | 文档切块（Markdown 章节 + PDF），为检索和 Wiki 发现提供基础 |
| `build-vector-index` | 向量索引构建，**支持混合检索**（词法 BM25 + 向量语义） |

### 2.3 Wiki 体系设计（基于 Karpathy LLM Wiki 模式）⭐⭐

#### 2.3.1 设计理念

参考 [Andrej Karpathy 的 LLM Wiki 模式](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f)：

> **"编译器而非解释器"** — 将知识提前编译为结构化的交叉链接 Markdown Wiki，而不是每次查询都做一次 RAG 检索。
>
> 三层架构：
> - `raw/`（原始文档）→ 人写入，LLM 只读
> - `wiki/`（知识页面）→ LLM 写入，人阅读
> - `CLAUDE.md`（Schema）→ 人配置，LLM 遵循

对应到 Iris 3.0：
- `SOURCE/` = raw/（原始文档存储地，人写入 + Iris 自动归档）
- `LLM-WIKI/` = wiki/（结构化知识库，LLM 写入，人阅读/编辑）
- `config/wiki.json` = Schema（页面类型、生成规则）

#### 2.3.2 页面类型（4 种）

| 类型 | 目录 | 说明 | 对应 Karpathy |
|------|------|------|---------------|
| **领域** (domain) | `01-领域/` | 某个工作领域的综合知识地图——技术体系、业务全景、方法论 | overview |
| **概念** (concept) | `02-概念/` | 核心概念、术语、方法、框架的精确定义和说明 | concept |
| **项目** (project) | `03-项目/` | 具体项目/专项的知识沉淀——目标、方案、进展、关键决策 | entity |
| **人物** (person) | `04-人物/` | 团队成员、组织角色、专长领域、负责方向 | entity |

#### 2.3.3 目录结构

```
LLM-WIKI/
├── index.md                    # 总索引页（LLM 维护，列出所有页面+摘要）
├── changelog.md                # 变更日志（追加式，记录每次生成/更新操作）
├── 01-领域/
│   ├── 领域-搜索推荐技术体系.md
│   ├── 领域-数据治理平台.md
│   └── ...
├── 02-概念/
│   ├── 概念-MMoE架构.md
│   ├── 概念-倒排索引.md
│   └── ...
├── 03-项目/
│   ├── 项目-质检自动化.md
│   ├── 项目-乾数据平台.md
│   └── ...
└── 04-人物/
    ├── 人物-张三.md
    └── ...
```

#### 2.3.4 命名规范

**文件命名**：`{类型前缀}-{名称}.md`

| 类型 | 前缀 | 示例 |
|------|------|------|
| 领域 | `领域-` | `领域-搜索推荐技术体系.md` |
| 概念 | `概念-` | `概念-MMoE架构.md` |
| 项目 | `项目-` | `项目-质检自动化.md` |
| 人物 | `人物-` | `人物-张三.md` |
| 索引页 | — | `index.md` |
| 变更日志 | — | `changelog.md` |

**目录命名**：`{序号}-{类型名}/`（序号前缀保持 Obsidian 中排序）

**页面内要求**：
- YAML frontmatter：`title` / `type` / `status`(draft/stable/review) / `created` / `updated` / `sources`
- 正文中使用 `[[Wiki-链接]]` 语法做交叉引用
- 每个页面的前 1-2 句为摘要（用于 `index.md` 和检索预览）

#### 2.3.5 页面模板

```markdown
---
title: 页面标题
type: domain|concept|project|person
status: draft          # draft | stable | review
sync: false            # 是否允许推送到飞书团队知识库
created: 2026-06-25
updated: 2026-06-25
sources:
  - SOURCE/path/to/source.md
---

## 摘要
1-2 句概述。

## 正文
...

## 关联页面
[[领域-相关领域]]
[[概念-相关概念]]

## 参考来源
- SOURCE/path/to/source1.md
- SOURCE/path/to/source2.md
```

#### 2.3.6 关键辅助文件

| 文件 | 说明 |
|------|------|
| `index.md` | 总索引——列出所有 Wiki 页面（按领域分组）+ 一行摘要。LLM 读取此文件决定加载哪些页面 |
| `changelog.md` | 变更日志——追加式记录每次 ingest/discover/lint 操作。格式：`YYYY-MM-DD [操作类型] 说明` |

### 2.4 Wiki 操作命令重构

| 需求 | 说明 |
|------|------|
| `discover-wiki` | 适配 4 种页面类型和层级结构，增量发现模式 |
| `build-wiki` | 适配新模板（4 种类型），支持单页/批量/审核模式 |
| `build-wiki-nav` | → 改为维护 `index.md`（总索引页，按领域分组） |
| `wiki-pipeline` | 发现→审核→生成全流程 |
| **新增 `wiki-lint`** | 定期健康检查——扫描矛盾、孤立页、缺失交叉引用（参考 Karpathy 的 lint 操作） |

### 2.6 Wiki 数据迁移

| 需求 | 说明 |
|------|------|
| 原始文档迁移 | **手动迁移**：用户自行从 `xiaolongxia/` 选择有价值的文档复制到 `SOURCE/` 对应目录 |
| Wiki 重建 | 新目录结构下重新运行发现和生成（旧 118 页保留不迁） |
| 旧 Wiki 存档 | v2.7.1 Wiki 页面保留原位置，新项目不引用 |

### 2.6 依赖 KB 的能力重构

| 需求 | 说明 |
|------|------|
| `search` 重构 | 关键词+向量混合检索，适配新 Wiki 4 种页面类型和层级结构 |
| `ask` 重构 | 检索增强问答，先读 `index.md` 确定相关页面→加载页面→回答 |
| `build-report` / `build-mindmap` 重构 | 复用新检索链路 |
| `transcribe-meeting` 重构 | 会议转录→纪要，从新 Wiki 加载领域词汇做 ASR 校正 |
| `build-asr-prompt` 重构 | 从新 Wiki 提取词汇表生成 ASR 校正提示词 |
| `build-biweekly-report` 重构 | 从新 Wiki 查询概念边界过滤素材 |
| `daily-start` 重构 | 扫描→切块→Wiki 自动发现/维护 |
| `status`/`diagnose` 补全 | 补充新 Wiki 健康度检查（页面数、index 完整性、孤立页等） |

---

## 步骤 3：飞书团队知识库管理 ⭐⭐（全新能力）

**目标**：新增飞书知识库操作能力，打通个人沉淀→团队共享链路。
**飞书接口层**：lark-cli

### 3.1 集成基础

| 需求 | 说明 |
|------|------|
| 飞书配置项 | `config/` 新增飞书知识库配置（复用 `${LARK_APP_ID}` / `${LARK_APP_SECRET}` 安全方案） |
| 飞书客户端 | 基于 lark-cli 封装飞书知识库 API |
| 飞书鉴权 | 支持 `lark-cli --as` 身份切换 |

### 3.2 个人→团队知识同步（手动触发）

| 需求 | 说明 |
|------|------|
| **手动触发** | 通过 CLI 命令（如 `sync-to-feishu`）手动发起同步，可选择单页或批量 |
| **冲突处理** | 飞书端已存在时，默认跳过，支持 `--overwrite` 覆盖 |

#### 同步筛选标准（建议）

采用 **三层筛选机制**，兼顾隐私保护和灵活控制：

| 层级 | 规则 | 说明 |
|------|------|------|
| **① 显式标记（白名单）** | 页面 YAML frontmatter 设置 `sync: true` 才可推送 | 默认 `false`，用户主动标记后才进入候选 |
| **② 状态检查** | 仅 `status: stable` 或 `status: review` 可推送 | `draft` 或未设置状态的页面跳过 |
| **③ 类型安全** | `person`（人物）类型默认不推送 | 含个人信息，除非显式标记 `sync: true` |

> 标记示例：
> ```yaml
> ---
> title: 搜索推荐技术体系
> type: domain
> status: stable
> sync: true          # ← 显式标记允许推送到飞书
> ---
> ```

### 3.3 团队知识查询

| 需求 | 说明 |
|------|------|
| **飞书知识库检索** | 调用飞书知识库 API 检索并回答问题 |
| **混合检索** | 本地 + 飞书搜索结果按相关性融合 |

### 3.4 知识发布通知（通过飞书 Bot）

| 需求 | 说明 |
|------|------|
| **集成方案** | 在 `config/` 中配置飞书 Bot 的 webhook URL 或凭据，通过 Bot 发送通知 |
| **新知识通知** | 同步成功后在目标群发送群聊卡片，包含页面标题、摘要、飞书链接 |
| **更新通知** | 已有知识更新时，发送更新通知 |
| **失败通知** | 同步失败时通知用户 |

---

## 项目目录结构

```
iris3/
├── .env.example          # 环境变量模板（版本控制）
├── .gitignore
├── CLAUDE.md             # 项目执行说明
├── LICENSE
├── pyproject.toml
├── README.md
├── config/               # 配置（*.json gitignored, *.example 版本控制）
│   ├── app.json.example
│   ├── llm.json.example
│   ├── data_source.json.example   # 步骤 2 完善（指向 SOURCE/）
│   ├── wiki.json.example          # 步骤 2 新增（指向 LLM-WIKI/）
│   └── ...（trello、weekly_report 等，按需添加）
├── data/                 # 运行时数据（全 gitignore）
├── src/
│   ├── iris/
│   │   ├── config/       # 配置加载（步骤 1）
│   │   ├── llm/          # LLM Provider + 路由（步骤 1）
│   │   ├── memory/       # 记忆系统（步骤 1）
│   │   ├── app/cli/      # CLI 框架（步骤 1）
│   │   ├── trello/       # Trello 集成（步骤 1）
│   │   ├── complex_input/ # 图文处理（步骤 1）
│   │   ├── ingest/       # 数据源扫描/切块（步骤 2）
│   │   ├── retrieval/    # 检索（步骤 2）
│   │   ├── qa/           # 问答（步骤 2）
│   │   ├── wiki/         # Wiki 体系（步骤 2）
│   │   ├── analysis/     # 报告/思维导图/双周报（步骤 2）
│   │   ├── feishu/       # 飞书知识库（步骤 3）
│   │   ├── core/         # 核心抽象（步骤 1）
│   │   └── utils/        # 工具（步骤 1）
│   └── ...
├── scripts/              # CLI 入口脚本
├── templates/            # Prompt/页面模板（步骤 2 增加 4 种页面模板）
├── tests/                # 测试
└── memory/               # 持久记忆（Claude 工作记忆）
```

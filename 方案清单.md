# Iris 3.0 升级方案清单

> 基于 Iris v2.7.1 能力盘点，确定新项目需求范围

---

## 一、项目定位

**Iris 3.0** 是由 Iris v2.7.1 重构升级而来，替换现有版本。定位不变：工作知识助手。核心变化是：
- 个人知识库（本地 Obsidian Wiki）体系重新设计
- 新增飞书团队知识库操作能力
- 打通个人沉淀 → 团队共享的链路

## 二、确认事项

| # | 决策 | 状态 |
|---|------|------|
| 1 | **新开项目**，不动当前 Iris v2.7.1 | ✅ |
| 2 | **新项目完成后替换**当前 Iris | ✅ |
| 3 | **能力保留，代码重写**（重搭骨架） | ✅ |
| 4 | **只迁移原始文档**到新知识库，旧 Wiki 页面（118页）保留不迁 | ✅ |
| 5 | **新知识库放在 Obsidian 仓库内**，独立目录，多端同步 | ✅ |
| 6 | **旧 LLM-Wiki 保留不动**，作为历史存档 | ✅ |
| 7 | **开发阶段私有仓库**，完成后替换公开仓库 | ✅ |

## 三、能力模块处置方案

### 3.1 日常工作流

| 能力 | 处置 | 说明 |
|------|------|------|
| `status` | 🔄 保留 | 检查配置、数据源状态等 |
| `diagnose` | 🔄 保留 | 系统诊断 |
| `check-config` | 🔄 保留 | 检查配置 |
| `daily-start` | ⬆️ 升级 | 增加飞书知识库状态同步 |
| `route-model` | 🔄 保留 | 查看模型路由 |

### 3.2 文档处理管道

| 能力 | 处置 | 说明 |
|------|------|------|
| `scan-source` | 🔄 保留 | 扫描原始 Markdown 文档 |
| `build-chunks` | 🔄 保留 | 文档切块（为检索和 Wiki 发现提供基础） |
| `build-vector-index` | ⬆️ 升级 | 向量索引，考虑支持混合检索 |

### 3.3 搜索问答

| 能力 | 处置 | 说明 |
|------|------|------|
| `search` | 🔄 保留 | 关键词/向量检索 |
| `ask` | 🔄 保留 | LLM 问答（检索增强） |
| `process` | 🔄 保留 | 图文混合双阶段处理 |

### 3.4 知识库（本地 Wiki）⭐⭐ 核心升级

| 能力 | 处置 | 说明 |
|------|------|------|
| Wiki 页面类型 | ⬆️ 升级 | 从 3 种（主题/项目/术语）扩充 |
| Wiki 目录结构 | ⬆️ 升级 | 从扁平改为层级，按领域归类 |
| `discover-wiki` | ⬆️ 升级 | 适配新结构和新的页面类型 |
| `build-wiki` | ⬆️ 升级 | 适配新模板和新类型 |
| `wiki-pipeline` | ⬆️ 升级 | 发现→审核→生成全流程 |
| `build-wiki-nav` | ⬆️ 升级 | 导航页改为按领域分组 |

### 3.5 飞书团队知识库操作 ⭐⭐ 全新能力

| 能力 | 说明 |
|------|------|
| 自动归档文档 | 处理后的文档自动归入飞书知识库正确目录 |
| 个人→团队同步 | 本地 Wiki 筛选后推送到飞书知识库 |
| 团队知识查询 | 调用飞书知识库检索回答问题 |
| 知识发布通知 | 新知识/更新时通知团队成员 |

### 3.6 报告生成

| 能力 | 处置 | 说明 |
|------|------|------|
| `build-report` | 🔄 保留 | 专题分析报告 |
| `build-mindmap` | 🔄 保留 | 思维导图 |
| `build-biweekly-report` | 🔄 保留 | 双周报生成 |

### 3.7 会议相关

| 能力 | 处置 | 说明 |
|------|------|------|
| `transcribe-meeting` | 🔄 保留 | 会议转写处理 |
| `pmo-meeting-process` | 🔄 保留 | PMO 会议纪要提取 |

### 3.8 ASR 校正（常用工具）

| 能力 | 处置 | 说明 |
|------|------|------|
| `build-asr-prompt` | ⬆️ 升级 | 适配新 Wiki 结构，从新知识库中提取领域词汇 |

### 3.9 记忆系统

| 能力 | 处置 | 说明 |
|------|------|------|
| `sync-memory` | 🔄 保留 | 记忆同步 |
| 记忆管理命令集 | 🔄 保留 | status/list/delete/export/import |

### 3.10 第三方集成

| 能力 | 处置 | 说明 |
|------|------|------|
| `trello` | 🔄 保留 | 看板 CRUD 与 LLM 汇总 |
| `extract-weekly-reports` | 🔄 保留 | 飞书邮箱周报提取 |
| `extract-didi-travel` | 🤔 待定 | 滴滴行程提取（低频，讨论是否保留） |

## 四、待讨论的问题

### 4.1 能力相关

1. **`extract-didi-travel`** — 低频使用，是否去掉？
2. **ASR 校正** — 升级后是继续从本地 Wiki 提取，还是从飞书知识库提取？或者两者都支持？

### 4.2 知识库结构（后续重点讨论）

1. 新 Wiki 的页面类型有哪些？
2. 目录层级怎么设计？
3. 命名规范如何统一？

### 4.3 飞书操作（后续重点讨论）

1. 自动归档的触发时机（即时/批量/定时）？
2. 个人→团队同步的筛选标准？
3. 通知方式（群聊卡片/私信）？

### 4.4 技术相关

1. 是否保持 Python 技术栈？
2. 是否继续使用 lark-cli 作为飞书接口层？
3. 新仓库名称（GitHub/Gitee）？

## 五、项目目录结构（草案）

```
iris3/
├── .env              # 密钥和路径变量
├── CLAUDE.md         # 项目执行说明
├── config/           # 配置（按需）
├── data/             # 运行时数据
├── src/              # 代码
├── templates/        # 模板
└── output/           # 输出产物
```

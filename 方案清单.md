# Iris 3.4 升级方案清单

> 基于 Iris v2.7.1 能力盘点，确定新项目需求范围
> **状态：步骤 1 ✅ / 步骤 2 ✅ / 步骤 3 ✅ 完成**
> 当前版本：**v3.4.0**（2026-06-27）

---

## 一、项目定位

**Iris 3.4** 是由 Iris v2.7.1 重构升级而来，替换现有版本。定位不变：工作知识助手。核心变化是：
- 个人知识库（本地 Obsidian Wiki）体系重新设计
- 新增飞书团队知识库操作能力（方向已调整为：飞书→本地提炼）
- 打通个人沉淀 → 团队共享的链路（本地→飞书同步已取消）

## 二、确认事项

| # | 决策 | 状态 |
|---|------|------|
| 1 | **新开项目**，不动当前 Iris v2.7.1 | ✅ |
| 2 | **新项目完成后替换**当前 Iris | ✅ |
| 3 | **能力保留，代码重写**（重搭骨架） | ✅ |
| 4 | **只迁移原始文档**到新知识库，旧 Wiki 页面（118页）保留不迁 | ✅ |
| 5 | **新知识库放在 Obsidian 仓库内**，独立目录，多端同步 | ✅ |
| 6 | **旧 LLM-Wiki 保留不动**，作为历史存档 | ✅ |
| 7 | **开发阶段私有仓库**，完成后替换公开仓库 | ✅ |

## 三、能力模块处置方案

### 3.1 日常工作流

| 能力 | 处置 | 说明 |
|------|------|------|
| `status` | 🔄 保留 | 检查配置、数据源状态等 |
| `diagnose` | 🔄 保留 | 系统诊断 |
| `check-config` | 🔄 保留 | 检查配置 |
| `daily-start` | ⬆️ 升级 | 增加飞书知识库状态同步 |
| `route-model` | 🔄 保留 | 查看模型路由 |

### 3.2 文档处理管道

| 能力 | 处置 | 说明 |
|------|------|------|
| `scan-source` | 🔄 保留 | 扫描原始 Markdown 文档 |
| `build-chunks` | 🔄 保留 | 文档切块（为检索和 Wiki 发现提供基础） |
| `build-vector-index` | ⬆️ 升级 | 向量索引，考虑支持混合检索 |

### 3.3 搜索问答

| 能力 | 处置 | 说明 |
|------|------|------|
| `search` | 🔄 保留 | 关键词/向量检索 |
| `ask` | 🔄 保留 | LLM 问答（检索增强） |
| `process` | 🔄 保留 | 图文混合双阶段处理 |

### 3.4 知识库（本地 Wiki）⭐⭐ 核心升级

| 能力 | 处置 | 说明 |
|------|------|------|
| Wiki 页面类型 | ⬆️ 升级 | 从 3 种（主题/项目/术语）扩充 |
| Wiki 目录结构 | ⬆️ 升级 | 从扁平改为层级，按领域归类 |
| `discover-wiki` | ⬆️ 升级 | 适配新结构和新的页面类型 |
| `build-wiki` | ⬆️ 升级 | 适配新模板和新类型 |
| `wiki-pipeline` | ⬆️ 升级 | 发现→审核→生成全流程 |
| `build-wiki-nav` | ⬆️ 升级 | 导航页改为按领域分组 |

### 3.5 飞书团队知识库操作 ⭐⭐ 全新能力（方向已调整）

| 能力 | 说明 | 状态 |
|------|------|------|
| 自动归档文档 | 处理后的文档自动归入飞书知识库正确目录 | ❌ 取消 |
| 个人→团队同步 | 本地 Wiki 筛选后推送到飞书知识库 | ❌ 取消（sync-to-feishu） |
| 团队知识查询 | 调用飞书知识库检索回答问题 | ❌ 取消（飞书原生可搜） |
| 知识发布通知 | 新知识/更新时通知团队成员 | ❌ 取消 |

> **v3.3 方向调整**：飞书集成方向由「本地→飞书发布」调整为「飞书→本地提炼」。
> 实际实现：`feishu-doc-convert`（文档→本地 Markdown）+ `chat-digest`（聊天记录 AI 提炼）

### 3.6 报告生成

| 能力 | 处置 | 说明 |
|------|------|------|
| `build-report` | 🔄 保留 | 专题分析报告 |
| `build-mindmap` | 🔄 保留 | 思维导图 |
| `build-biweekly-report` | 🔄 保留 | 双周报生成 |

### 3.7 会议相关

| 能力 | 处置 | 说明 |
|------|------|------|
| `transcribe-meeting` | 🔄 保留 | 会议转写处理 |
| `pmo-meeting-process` | 🔄 保留 | PMO 会议纪要提取 |

### 3.8 ASR 校正（常用工具）

| 能力 | 处置 | 说明 |
|------|------|------|
| `build-asr-prompt` | ⬆️ 升级 | 适配新 Wiki 结构，从新知识库中提取领域词汇 |

### 3.9 记忆系统

| 能力 | 处置 | 说明 |
|------|------|------|
| `sync-memory` | 🔄 保留 | 记忆同步 |
| 记忆管理命令集 | 🔄 保留 | status/list/delete/export/import |

### 3.10 第三方集成

| 能力 | 处置 | 说明 |
|------|------|------|
| `trello` | 🔄 保留 | 看板 CRUD 与 LLM 汇总 |
| `extract-weekly-reports` | 🔄 保留 | 飞书邮箱周报提取 |
| `extract-didi-travel` | ✅ 保留 | 滴滴行程提取（委托脚本） |

## 四、已讨论的问题（全部已解决）

### 4.1 能力相关

1. **`extract-didi-travel`** — ✅ 保留，作为委托脚本
2. **ASR 校正** — ✅ 从本地 Wiki 提取（v3.4.1 build-asr-prompt 重写）
3. **`sync-memory`** — ✅ 保留，作为委托脚本

### 4.2 知识库结构（全部确定）

1. 新 Wiki 的页面类型：✅ 4 种（domain/concept/project/person）
2. 目录层级：✅ `LLM-WIKI/` → `01-领域/`, `02-概念/`, `03-项目/`, `04-人物/`
3. 命名规范：✅ `{类型前缀}-{名称}.md`，YAML frontmatter

### 4.3 飞书操作（方向调整）

1. ✅ 方向由「本地→飞书同步」调整为「飞书→本地提炼」
2. ✅ 本地→飞书发布（sync-to-feishu）已取消
3. ✅ 团队知识查询已取消（飞书原生可搜）

### 4.4 技术相关（已确定）

1. ✅ 保持 Python 3.9+ 技术栈
2. ✅ 继续使用 lark-cli 作为飞书接口层
3. ✅ 仓库名称：iris3

## 五、项目目录结构（实际）

```
iris3/
├── pyproject.toml          # 3.4.0，依赖：PyMuPDF / python-docx / numpy
├── README.md
├── CLAUDE.md               # 项目执行说明
├── .env.example            # 环境变量模板
├── config/                 # 配置（*.json gitignored, *.example 版本控制）
│   ├── app.json / .example
│   ├── llm.json / .example
│   ├── data_source.json / .example
│   ├── wiki.json / .example
│   ├── meeting_routes.json / .example
│   ├── feishu_ingest.json / .example
│   ├── trello.json / .example
│   ├── wiki_discovery_rules.json / .example
│   └── weekly_report.json / .example
├── data/                   # 运行时数据（全 gitignore）
│   ├── metadata/           # 扫描摘要、chunk 摘要、向量索引
│   ├── dedup/              # 排重索引（feishu_doc / chat_digest）
│   └── ...
├── src/iris/
│   ├── __init__.py         # 协议版本 3.4
│   ├── config/             # 配置加载 + 密钥链
│   ├── llm/                # LLM Provider + 路由 + 模型管理
│   ├── memory/             # 记忆系统（画像/校正/工作上下文/会话/自治维护）
│   ├── core/               # 核心抽象（Protocol/锁/写保护/存储/Agent适配/脚本加载）
│   ├── app/cli/            # CLI 框架（argparse 分发表，39 handler + 4 委托脚本）
│   ├── app/transcribe_meeting/ # 会议转录管线
│   ├── trello/             # Trello 看板集成
│   ├── complex_input/      # 图文混合双阶段处理
│   ├── utils/              # 工具（日志/校验/提示词/token化）
│   ├── ingest/             # 数据源扫描/切块/PDF提取
│   ├── retrieval/          # 混合检索（BM25+向量/查询规划/增强检索）
│   ├── qa/                 # 问答服务（本地/LLM模式+记忆联动）
│   ├── wiki/               # Wiki 体系（发现/生成/导航/搜索/术语提取/上下文加载）
│   ├── analysis/           # 报告/思维导图/双周报
│   ├── output/             # 输出格式化（Markdown/DOCX）
│   └── feishu/             # 飞书集成（客户端/文档转换/聊天提炼）
├── scripts/                # CLI 入口 + 委托脚本（trello/周报/滴滴/记忆同步）
├── templates/              # Prompt 模板 + Wiki 页面模板（4 种类型）
├── tests/                  # 单元测试（97 用例，6 文件）
└── memory/                 # Claude 工作记忆
```
